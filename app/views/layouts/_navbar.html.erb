<nav class="navbar is-light" role="navigation" aria-label="main navigation">
  <div class="container">
    <div class="navbar-brand">
      <%= link_to "focus field", root_url, class: "navbar-item" %>
    </div>
    <div class="navbar-end">
    <% if logged_in? %>
      <div class="navbar-item">
        <%= button_to "sign out", sessions_path, method: :delete, class: "button" %>
      </div>
    <% else %>
      <div class="navbar-item">
        <%= link_to "sign in", new_sessions_path, class: "button is-primary" %>
      </div>
    <% end %>
    </div>
  </div>
</nav>
